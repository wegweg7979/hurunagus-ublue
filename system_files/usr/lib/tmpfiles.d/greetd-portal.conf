# Recreate the greeter's xdg-desktop-portal disable symlink at boot
# (baked into the image by the greeter package; see bootc container lint)
L /var/lib/greetd/.config/systemd/user/xdg-desktop-portal.service - - - - /dev/null
